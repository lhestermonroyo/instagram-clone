export const styles = (theme) => ({
  suggestionContainer: {
    padding: theme.spacing(1),
  },
  title: {
    textDecoration: 'none',
  },
  followButton: {
    color: '#0095F6',
    marginTop: theme.spacing(1),
  },
});
